export interface MensagemAlerta {
    status: string
    mensagem: string
}

export interface Musicas {
    id: number,
    imagem: string,
    musica: string,
    compositor: string,
    som: string
    demonstrando: boolean
}